# API Reference

Every exported symbol, characterized by what it does on representative inputs. This page generates itself from docstrings — write a good docstring and the reference maintains itself.

```@autodocs
Modules = [StrictTemplate]
```
